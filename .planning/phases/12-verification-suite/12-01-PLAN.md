---
phase: 12-verification-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/query/verify.ts
  - sdk/src/query/verify.test.ts
  - sdk/src/query/frontmatter.ts
  - sdk/src/query/frontmatter.test.ts
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03]

must_haves:
  truths:
    - "Running gsd-sdk query verify.plan-structure on a PLAN.md returns valid/invalid with errors, warnings, task_count, tasks, frontmatter_fields"
    - "Running gsd-sdk query verify.phase-completeness on a phase number returns complete/incomplete with plan_count, summary_count, incomplete_plans, orphan_summaries"
    - "Running gsd-sdk query verify.artifacts on a PLAN.md checks must_haves.artifacts for file existence, min_lines, contains, exports"
    - "parseMustHavesBlock is exported from frontmatter.ts and correctly parses 3-level YAML nesting"
  artifacts:
    - path: "sdk/src/query/verify.ts"
      provides: "verifyPlanStructure, verifyPhaseCompleteness, verifyArtifacts handlers"
      exports: ["verifyPlanStructure", "verifyPhaseCompleteness", "verifyArtifacts"]
    - path: "sdk/src/query/verify.test.ts"
      provides: "Unit tests for verify handlers"
      min_lines: 100
    - path: "sdk/src/query/frontmatter.ts"
      provides: "parseMustHavesBlock export added"
      exports: ["parseMustHavesBlock"]
  key_links:
    - from: "sdk/src/query/verify.ts"
      to: "sdk/src/query/frontmatter.ts"
      via: "import parseMustHavesBlock"
      pattern: "import.*parseMustHavesBlock.*from.*frontmatter"
---

<objective>
Port plan structure validation, phase completeness checking, and artifact verification handlers from verify.cjs to TypeScript SDK, plus port parseMustHavesBlock utility from frontmatter.cjs.

Purpose: Delivers VERIFY-01 (plan structure), VERIFY-02 (phase completeness), and VERIFY-03 (artifact checks) as native TypeScript query handlers, plus the shared parseMustHavesBlock utility needed by VERIFY-03 and VERIFY-04.

Output: verify.ts with 3 handlers, verify.test.ts with tests, parseMustHavesBlock added to frontmatter.ts
</objective>

<execution_context>
@D:/Repos/get-shit-done/.claude/get-shit-done/workflows/execute-plan.md
@D:/Repos/get-shit-done/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-verification-suite/12-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From sdk/src/query/utils.ts:
```typescript
export interface QueryResult { data: unknown; }
export type QueryHandler = (args: string[], projectDir: string) => Promise<QueryResult>;
```

From sdk/src/errors.ts:
```typescript
export enum ErrorClassification { Validation = 'validation', Execution = 'execution', Blocked = 'blocked', Interruption = 'interruption' }
export class GSDError extends Error { readonly classification: ErrorClassification; constructor(message: string, classification: ErrorClassification); }
```

From sdk/src/query/frontmatter.ts:
```typescript
export function extractFrontmatter(content: string): Record<string, unknown>;
export function stripFrontmatter(content: string): string;
export const frontmatterGet: QueryHandler;
```

From sdk/src/query/helpers.ts:
```typescript
export function normalizePhaseName(phase: string): string;
export function planningPaths(projectDir: string): PlanningPaths;
export function phaseTokenMatches(dirName: string, normalized: string): boolean;
export function toPosixPath(p: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port parseMustHavesBlock to frontmatter.ts and add verifyPlanStructure handler</name>
  <files>sdk/src/query/frontmatter.ts, sdk/src/query/frontmatter.test.ts, sdk/src/query/verify.ts, sdk/src/query/verify.test.ts</files>
  <read_first>
    - sdk/src/query/frontmatter.ts
    - sdk/src/query/frontmatter.test.ts
    - get-shit-done/bin/lib/frontmatter.cjs (lines 195-295 for parseMustHavesBlock)
    - get-shit-done/bin/lib/verify.cjs (lines 108-167 for cmdVerifyPlanStructure)
    - sdk/src/query/utils.ts (QueryHandler, QueryResult types)
    - sdk/src/errors.ts (GSDError, ErrorClassification)
  </read_first>
  <behavior>
    parseMustHavesBlock:
    - Test: parseMustHavesBlock with artifacts block containing path, provides, min_lines, contains, exports returns structured objects
    - Test: parseMustHavesBlock with key_links block containing from, to, via, pattern returns structured objects
    - Test: parseMustHavesBlock with simple string items (truths) returns string array
    - Test: parseMustHavesBlock with nested array values (exports: [a, b]) preserves array
    - Test: parseMustHavesBlock with missing block returns empty array
    - Test: parseMustHavesBlock with no frontmatter returns empty array
    - Test: parseMustHavesBlock with content lines but 0 parsed items emits diagnostic warning in result

    verifyPlanStructure:
    - Test: valid plan with all required frontmatter fields and task elements returns { valid: true, errors: [], task_count: N }
    - Test: plan missing required frontmatter field 'wave' returns { valid: false, errors: ['Missing required frontmatter field: wave'] }
    - Test: plan with task missing <name> element returns error
    - Test: plan with task missing <action> element returns error
    - Test: plan with wave > 1 but empty depends_on returns warning
    - Test: plan with checkpoint task but autonomous !== false returns error
    - Test: plan with no tasks returns warning 'No <task> elements found'
    - Test: missing file returns { error: 'File not found' }
    - Test: no args throws GSDError with Validation classification
  </behavior>
  <action>
    1. Add `parseMustHavesBlock` to `sdk/src/query/frontmatter.ts`. Port from `frontmatter.cjs` lines 195-295. Key behaviors:
       - Extract raw YAML from first `---\n...\n---` block
       - Find `must_haves:` at its indentation level
       - Find the requested `blockName:` indented under must_haves
       - Parse list items with `- key: value` syntax, continuation KVs, nested arrays
       - Return `[]` for missing block or no frontmatter
       - Include a `warnings` array in a structured return: `{ items: T[], warnings: string[] }` — when blockLines exist but 0 items parsed, add diagnostic warning string (instead of CJS stderr.write)
       - Export the function

    2. Create `sdk/src/query/verify.ts` with `verifyPlanStructure` handler. Port from `verify.cjs` lines 108-167:
       - arg[0] = file path (required, throw GSDError Validation if missing)
       - Resolve path: isAbsolute ? use as-is : join(projectDir, path)
       - Read file with readFile (return `{ error: 'File not found' }` if missing)
       - Extract frontmatter with `extractFrontmatter`
       - Check 8 required fields: phase, plan, type, wave, depends_on, files_modified, autonomous, must_haves
       - Parse `<task>` XML elements with regex `/<task[^>]*>([\s\S]*?)<\/task>/g`
       - For each task check: `<name>` (error if missing), `<action>` (error if missing), `<verify>` (warning), `<done>` (warning), `<files>` (warning)
       - Wave/depends_on consistency: wave > 1 with empty depends_on = warning
       - Checkpoint/autonomous consistency: `/<task\s+type=["']?checkpoint/` present but autonomous not false = error
       - Return `{ valid, errors, warnings, task_count, tasks, frontmatter_fields }`

    3. Write tests FIRST (RED), then implement (GREEN). Use mkdtemp pattern for temp files when needed for file-based tests. For parseMustHavesBlock, test with inline content strings (no temp files needed).
  </action>
  <verify>
    <automated>cd sdk && npx vitest run --project unit src/query/frontmatter.test.ts src/query/verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `parseMustHavesBlock` exported from `sdk/src/query/frontmatter.ts` — grep: `export function parseMustHavesBlock`
    - `verifyPlanStructure` exported from `sdk/src/query/verify.ts` — grep: `export const verifyPlanStructure`
    - parseMustHavesBlock parses artifacts block with path, provides, min_lines into structured objects
    - parseMustHavesBlock returns `{ items: [], warnings: [] }` structure
    - verifyPlanStructure checks 8 required frontmatter fields: phase, plan, type, wave, depends_on, files_modified, autonomous, must_haves
    - verifyPlanStructure returns `{ valid, errors, warnings, task_count, tasks, frontmatter_fields }`
    - All tests pass
  </acceptance_criteria>
  <done>parseMustHavesBlock exported and tested. verifyPlanStructure handler validates plan structure matching CJS behavior. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add verifyPhaseCompleteness and verifyArtifacts handlers</name>
  <files>sdk/src/query/verify.ts, sdk/src/query/verify.test.ts</files>
  <read_first>
    - sdk/src/query/verify.ts (from Task 1)
    - sdk/src/query/verify.test.ts (from Task 1)
    - get-shit-done/bin/lib/verify.cjs (lines 169-213 for cmdVerifyPhaseCompleteness, lines 283-336 for cmdVerifyArtifacts)
    - sdk/src/query/phase.ts (findPhase handler pattern)
    - sdk/src/query/helpers.ts (normalizePhaseName, planningPaths)
  </read_first>
  <behavior>
    verifyPhaseCompleteness:
    - Test: phase with 2 plans and 2 matching summaries returns { complete: true, plan_count: 2, summary_count: 2 }
    - Test: phase with 2 plans and 1 summary returns { complete: false, incomplete_plans: ['09-02'] }
    - Test: phase with orphan summary returns warning
    - Test: phase not found returns { error: 'Phase not found' }
    - Test: no args throws GSDError Validation

    verifyArtifacts:
    - Test: plan with artifacts where all files exist and pass checks returns { all_passed: true }
    - Test: plan with artifact where file does not exist returns passed: false with 'File not found'
    - Test: plan with artifact where min_lines fails returns issue
    - Test: plan with artifact where contains check fails returns issue
    - Test: plan with artifact where exports check fails returns issue
    - Test: plan with no must_haves.artifacts returns error message
    - Test: no args throws GSDError Validation
  </behavior>
  <action>
    1. Add `verifyPhaseCompleteness` to `sdk/src/query/verify.ts`. Port from `verify.cjs` lines 169-213:
       - arg[0] = phase number (required, throw GSDError Validation if missing)
       - Use `planningPaths(projectDir)` to get phases dir, then scan for phase directory matching the phase number using `normalizePhaseName` and `phaseTokenMatches` (mirror the findPhase pattern from `sdk/src/query/phase.ts`)
       - Read directory with `readdir`
       - Filter files matching `-PLAN.md` and `-SUMMARY.md` suffixes (case-insensitive)
       - Extract IDs by stripping suffix from filenames
       - Compare: plans without summaries = errors (incomplete), summaries without plans = warnings (orphans)
       - Return `{ complete, phase, plan_count, summary_count, incomplete_plans, orphan_summaries, errors, warnings }`

    2. Add `verifyArtifacts` to `sdk/src/query/verify.ts`. Port from `verify.cjs` lines 283-336:
       - arg[0] = plan file path (required, throw GSDError Validation if missing)
       - Read plan file, call `parseMustHavesBlock(content, 'artifacts')`
       - If 0 artifacts found, return `{ error: 'No must_haves.artifacts found in frontmatter' }`
       - For each artifact object (skip strings): check `artifact.path` exists on disk
       - If exists: read file, check `min_lines` (line count), `contains` (string includes), `exports` (each export string present)
       - Build check object: `{ path, exists, issues: [], passed: boolean }`
       - Return `{ all_passed, passed, total, artifacts: results }`

    3. Write tests FIRST (RED), then implement (GREEN). Use mkdtemp + write fixture files for phase completeness tests. For artifacts, write temp plan files with frontmatter and temp artifact files.
  </action>
  <verify>
    <automated>cd sdk && npx vitest run --project unit src/query/verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `verifyPhaseCompleteness` exported from `sdk/src/query/verify.ts` — grep: `export const verifyPhaseCompleteness`
    - `verifyArtifacts` exported from `sdk/src/query/verify.ts` — grep: `export const verifyArtifacts`
    - verifyPhaseCompleteness matches PLAN files to SUMMARY files and reports incomplete/orphan
    - verifyArtifacts reads must_haves.artifacts from frontmatter and checks file existence, min_lines, contains, exports
    - All verify.test.ts tests pass
  </acceptance_criteria>
  <done>verifyPhaseCompleteness and verifyArtifacts handlers implemented and tested. Both match CJS behavior for plan/summary matching and artifact validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file path args -> file system | User-supplied paths used for readFile/readdir |
| plan content -> regex parsing | Untrusted plan content parsed with regex for XML task elements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | T (Tampering) | verifyPlanStructure file path | mitigate | Null byte rejection on file paths (existing frontmatterGet pattern) |
| T-12-02 | I (Info Disclosure) | verifyArtifacts | accept | File existence checks reveal path presence; acceptable since this is a local dev tool |
| T-12-03 | D (Denial of Service) | verifyPlanStructure regex | mitigate | Use non-greedy `[\s\S]*?` in task XML regex to avoid catastrophic backtracking |
| T-12-04 | T (Tampering) | parseMustHavesBlock | accept | Parses YAML from frontmatter; no write operations; input already under developer control |
</threat_model>

<verification>
- All verify.test.ts and frontmatter.test.ts tests pass
- `npx vitest run --project unit` full suite green
- parseMustHavesBlock handles 3-level nesting, empty blocks, and diagnostic warnings
- verifyPlanStructure output shape matches CJS: { valid, errors, warnings, task_count, tasks, frontmatter_fields }
- verifyPhaseCompleteness output shape matches CJS: { complete, phase, plan_count, summary_count, ... }
- verifyArtifacts output shape matches CJS: { all_passed, passed, total, artifacts }
</verification>

<success_criteria>
- 3 verification handlers exported from verify.ts
- parseMustHavesBlock exported from frontmatter.ts
- All handlers return typed JSON matching CJS output shape
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/12-verification-suite/12-01-SUMMARY.md`
</output>
