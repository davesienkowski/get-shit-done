---
phase: 13.1-upstream-reconciliation
plan: 01
subsystem: sdk
tags: [locking, atomicity, config-validation, toctou, concurrent-agents]

# Dependency graph
requires:
  - phase: 11-state-mutations
    provides: acquireStateLock, releaseStateLock, atomicWriteConfig, configSet, VALID_CONFIG_KEYS
provides:
  - Lock exit cleanup via _heldStateLocks + process.on('exit')
  - Graceful degradation on non-EEXIST lock errors
  - PID-qualified atomic config writes with rename fallback
  - Config lock protection for concurrent agent safety
  - Context value validation and CONFIG_KEY_SUGGESTIONS typo correction
  - Global defaults (~/.gsd/defaults.json) loading in configNewProject
affects: [phase-lifecycle, composition-retirement, golden-tests]

# Tech tracking
tech-stack:
  added: []
  patterns: [process-exit-cleanup, pid-qualified-temp-files, curated-suggestion-map]

key-files:
  created: []
  modified:
    - sdk/src/query/state-mutation.ts
    - sdk/src/query/state-mutation.test.ts
    - sdk/src/query/config-mutation.ts
    - sdk/src/query/config-mutation.test.ts

key-decisions:
  - "Reused acquireStateLock/releaseStateLock for config lock protection rather than implementing separate lock"
  - "Added CONFIG_KEY_SUGGESTIONS as first-pass check before LCP fallback for more precise typo correction"
  - "Global defaults merge uses three-level pattern: hardcoded <- globalDefaults <- userChoices"

patterns-established:
  - "Process exit cleanup: module-level Set tracking held resources with process.on('exit') handler"
  - "Curated suggestion map: explicit known-typo map checked before algorithmic fallback"

requirements-completed: [RECON-01, RECON-02]

# Metrics
duration: 4min
completed: 2026-04-08
---

# Phase 13.1 Plan 01: Upstream Reconciliation Summary

**Reconciled 9 SDK divergences (D2-D9, D11) with upstream v1.34.0 CJS: lock exit cleanup, PID-qualified atomic writes, config lock protection, context validation, typo suggestion map, and global defaults loading**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-08T09:49:49Z
- **Completed:** 2026-04-08T09:53:28Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Fixed state-mutation.ts locking: added _heldStateLocks Set with process.on('exit') cleanup (D2) and graceful degradation on non-EEXIST errors (D3)
- Fixed config-mutation.ts atomicity: PID-qualified temp file (D4), rename-failure fallback (D5), lock protection via acquireStateLock (D6)
- Fixed config-mutation.ts completeness: added learnings.max_inject and features.global_learnings keys (D7), context value validation (D8), CONFIG_KEY_SUGGESTIONS map (D9), and ~/.gsd/defaults.json loading (D11)
- All 1106 unit tests pass with zero regressions

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix state-mutation.ts locking divergences (D2, D3)**
   - `62f9ac8` (test: add failing tests for lock cleanup and non-EEXIST handling)
   - `b68ff29` (feat: fix state-mutation locking divergences D2 and D3)
2. **Task 2: Fix config-mutation.ts divergences (D4-D9, D11)**
   - `a18ffa0` (test: add failing tests for config-mutation divergences D4-D9, D11)
   - `fabbda8` (feat: fix config-mutation divergences D4-D9, D11)

_Note: TDD tasks have RED (test) + GREEN (feat) commits_

## Files Created/Modified
- `sdk/src/query/state-mutation.ts` - Added _heldStateLocks Set, process.on('exit') cleanup, non-EEXIST graceful return
- `sdk/src/query/state-mutation.test.ts` - Added tests for lock tracking and non-EEXIST handling
- `sdk/src/query/config-mutation.ts` - PID temp, rename fallback, config lock, missing keys, context validation, suggestion map, global defaults
- `sdk/src/query/config-mutation.test.ts` - Added tests for D4-D9, D11 behaviors

## Decisions Made
- Reused acquireStateLock/releaseStateLock for config lock protection rather than implementing a separate locking mechanism -- these functions already support arbitrary file paths
- Added CONFIG_KEY_SUGGESTIONS as first-pass check before LCP fallback, matching CJS behavior exactly
- Global defaults merge uses three-level pattern: hardcoded <- globalDefaults <- userChoices, matching CJS config.cjs:103-120

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Ready for Plan 13.1-02 (re-run golden tests and re-verify Phase 9-12 must-haves)
- All SDK code now matches upstream v1.34.0 CJS behavior for locking and config writes

---
*Phase: 13.1-upstream-reconciliation*
*Completed: 2026-04-08*
