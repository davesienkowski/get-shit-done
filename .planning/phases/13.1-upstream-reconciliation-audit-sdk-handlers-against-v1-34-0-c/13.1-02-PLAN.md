---
phase: 13.1-upstream-reconciliation
plan: 02
type: execute
wave: 2
depends_on:
  - 13.1-01
files_modified:
  - sdk/src/golden/golden.integration.test.ts
autonomous: true
requirements:
  - RECON-03

must_haves:
  truths:
    - "All 17 golden integration tests pass against updated gsd-tools.cjs"
    - "Phase 9-12 must-have artifacts still exist and key-links still match"
    - "config-set golden test still matches CJS output shape after lock+validation changes"
    - "PR #1959 acceptance criteria enforcement is workflow-only — no SDK changes needed"
  artifacts:
    - path: "sdk/src/golden/golden.integration.test.ts"
      provides: "Golden integration tests verifying SDK/CJS parity"
  key_links:
    - from: "sdk/src/golden/golden.integration.test.ts"
      to: "get-shit-done/bin/gsd-tools.cjs"
      via: "golden test comparison (SDK output vs CJS output)"
      pattern: "execSync.*gsd-tools"
---

<objective>
Re-run all 17 golden integration tests and re-verify Phase 9-12 must-haves against the current codebase after the Plan 01 divergence fixes.

Purpose: Validate that the SDK code changes from Plan 01 did not break CJS output parity (golden tests) and that all prior phase deliverables remain intact (must-haves).

Output: Green golden test suite confirming SDK/CJS parity; documented re-verification of Phase 9-12 must-haves.
</objective>

<execution_context>
@D:/Repos/get-shit-done/.claude/get-shit-done/workflows/execute-plan.md
@D:/Repos/get-shit-done/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-upstream-reconciliation-audit-sdk-handlers-against-v1-34-0-c/13.1-RESEARCH.md
@.planning/phases/13.1-upstream-reconciliation-audit-sdk-handlers-against-v1-34-0-c/13.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run golden integration tests and fix any output shape regressions</name>
  <files>sdk/src/golden/golden.integration.test.ts</files>
  <read_first>
    - sdk/src/golden/golden.integration.test.ts (full file — understand all 17 test cases and their assertions)
    - .planning/phases/13.1-upstream-reconciliation-audit-sdk-handlers-against-v1-34-0-c/13.1-RESEARCH.md (Golden Tests section — risk assessment per test)
    - .planning/phases/13.1-upstream-reconciliation-audit-sdk-handlers-against-v1-34-0-c/13.1-01-SUMMARY.md (what changed in Plan 01)
  </read_first>
  <action>
    1. Run the full golden integration test suite: `cd sdk && npx vitest run --project integration -x`
    2. If all 17 tests pass — done. The SDK output shape still matches CJS.
    3. If any test fails (most likely test #11 config-set due to lock changes):
       - Examine the failure output to determine if it is an output shape difference or a legitimate bug.
       - If the SDK now returns additional fields (e.g., from lock metadata), the golden test assertions may need updating to compare only the expected fields.
       - If the failure is a real bug introduced by Plan 01, fix it in the source file (config-mutation.ts or state-mutation.ts).
       - Re-run until all 17 pass.
    4. Do NOT change golden test assertions to paper over real behavioral differences. Only adjust if the SDK output is a superset of CJS output (extra fields are acceptable, missing/changed fields are not).
  </action>
  <verify>
    <automated>cd sdk && npx vitest run --project integration -x</automated>
  </verify>
  <acceptance_criteria>
    - Running `cd sdk && npx vitest run --project integration -x` exits with code 0
    - All 17 golden test cases pass (verify in test output: "17 passed" or "Tests: 17 passed")
  </acceptance_criteria>
  <done>All 17 golden integration tests pass, confirming SDK output still matches CJS after divergence fixes.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify Phase 9-12 must-haves against current codebase</name>
  <files>sdk/src/golden/golden.integration.test.ts</files>
  <read_first>
    - .planning/phases/13.1-upstream-reconciliation-audit-sdk-handlers-against-v1-34-0-c/13.1-RESEARCH.md (Phase Must-Haves Re-Verification section and Acceptance Criteria Enforcement section)
    - sdk/src/query/state-mutation.ts (verify acquireStateLock still exports correctly)
    - sdk/src/query/config-mutation.ts (verify configSet output shape unchanged)
  </read_first>
  <action>
    Re-verify that Phase 9-12 must-have artifacts still exist and function correctly after Plan 01 changes. This is a verification pass, not a code change task.

    **Phase 9 artifacts (5 checks):**
    1. `sdk/src/errors.ts` exports ErrorClassification enum — `grep "export.*ErrorClassification" sdk/src/errors.ts`
    2. `sdk/src/query/utils.ts` exports QueryHandler type — `grep "export.*QueryHandler" sdk/src/query/utils.ts`
    3. `sdk/src/cli.ts` has query subcommand routing — `grep "query" sdk/src/cli.ts`
    4. `sdk/src/golden/capture.ts` exists — `ls sdk/src/golden/capture.ts`
    5. Golden fixtures exist — `ls sdk/src/golden/fixtures/`

    **Phase 10 artifacts (4 checks):**
    6. `sdk/src/query/state.ts` exports stateLoad — `grep "export.*stateLoad" sdk/src/query/state.ts`
    7. `sdk/src/query/config-query.ts` exports configGet — `grep "export.*configGet" sdk/src/query/config-query.ts`
    8. `sdk/src/query/phase.ts` exports findPhase — `grep "export.*findPhase" sdk/src/query/phase.ts`
    9. `sdk/src/query/roadmap.ts` exports roadmapAnalyze — `grep "export.*roadmapAnalyze" sdk/src/query/roadmap.ts`

    **Phase 11 artifacts (4 checks — at-risk from Plan 01 changes):**
    10. `sdk/src/query/state-mutation.ts` exports acquireStateLock — `grep "export.*acquireStateLock" sdk/src/query/state-mutation.ts`
    11. `sdk/src/query/config-mutation.ts` exports configSet — `grep "export.*configSet" sdk/src/query/config-mutation.ts`
    12. State mutations emit events — `grep "GSDEventType" sdk/src/query/registry.ts`
    13. Template fill handler exists — `grep "template" sdk/src/query/registry.ts`

    **Phase 12 artifacts (3 checks):**
    14. `sdk/src/query/verify.ts` exports verifyPlanStructure — `grep "export.*verifyPlanStructure" sdk/src/query/verify.ts`
    15. `sdk/src/query/validate.ts` exports validateConsistency — `grep "export.*validateConsistency" sdk/src/query/validate.ts`
    16. `sdk/src/query/validate.ts` exports validateHealth — `grep "export.*validateHealth" sdk/src/query/validate.ts`

    **Acceptance criteria enforcement (PR #1959):**
    17. Confirm PR #1959 is workflow-only: `grep -l "acceptance_criteria\|acceptance criteria" get-shit-done/workflows/execute-plan.md` should find the enforcement. `grep "acceptance_criteria" sdk/src/` should return no matches (SDK does not enforce this — it is agent-level).

    Run all 16 artifact grep checks. If any fail, investigate whether Plan 01 accidentally removed an export or broke a file. Fix if needed.

    Run the full unit test suite as a final regression check: `cd sdk && npx vitest run --project unit`
  </action>
  <verify>
    <automated>cd sdk && npx vitest run --project unit -x</automated>
  </verify>
  <acceptance_criteria>
    - grep "export.*acquireStateLock" sdk/src/query/state-mutation.ts returns at least 1 match
    - grep "export.*configSet" sdk/src/query/config-mutation.ts returns at least 1 match
    - grep "export.*ErrorClassification" sdk/src/errors.ts returns at least 1 match
    - grep "export.*validateHealth" sdk/src/query/validate.ts returns at least 1 match
    - Running `cd sdk && npx vitest run --project unit -x` exits with code 0
  </acceptance_criteria>
  <done>All Phase 9-12 must-have artifacts verified present and functional. Full unit test suite passes. PR #1959 acceptance criteria enforcement confirmed as workflow-only (no SDK changes needed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK output -> golden test assertions | Test compares SDK JSON against CJS JSON — shape drift is the threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-06 | Repudiation | Golden test assertions | mitigate | Never weaken assertions to hide divergences — only add tolerance for extra fields |
| T-13.1-07 | Information Disclosure | Must-have grep checks | accept | Grep patterns are read-only verification, no data exposure risk |
</threat_model>

<verification>
- `cd sdk && npx vitest run --project integration -x` passes (17/17 golden tests)
- `cd sdk && npx vitest run --project unit -x` passes (full unit suite)
- All 16 Phase 9-12 artifact grep checks return matches
</verification>

<success_criteria>
All 17 golden tests pass. All Phase 9-12 must-have artifacts verified present. Full unit test suite green. PR #1959 confirmed workflow-only.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-upstream-reconciliation-audit-sdk-handlers-against-v1-34-0-c/13.1-02-SUMMARY.md`
</output>
