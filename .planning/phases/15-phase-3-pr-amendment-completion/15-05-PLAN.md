---
phase: 15-phase-3-pr-amendment-completion
plan: "05"
type: feature
wave: 1
depends_on: []
files_modified:
  - sdk/src/query/**/*.ts
  - sdk/src/**/*.ts
  - sdk/docs/**/*.md
  - sdk/README.md
  - agents/**/*.md
  - get-shit-done/workflows/**/*.md
  - docs/**/*.md
  - CHANGELOG.md
autonomous: false
must_haves:
  truths:
    - "Umbrella issue #2302 acceptance criteria satisfied across amended PRs #2340-#2343 with no Phase 3.5 deferral"
    - "Universal CJS fallback policy implemented per design (registry + CLI + GSDTools) with tests"
    - "Stub tests replaced with real coverage for profile, skills, websearch; E2E + error-classification tests exist"
    - "sdk/README.md and sdk/docs/{PARITY,GOLDEN-TESTS,ARCHITECTURE}.md exist; sdk/HANDOVER-*.md retired from package"
  artifacts:
    - path: "sdk/src/query/QUERY-HANDLERS.md"
      provides: "Accurate handler matrix without misleading essentially-all language"
    - path: "sdk/src/cli.ts"
      provides: "CLI unknown-command fallback to gsd-tools.cjs with stderr warning"
    - path: "sdk/src/phase-runner.ts"
      provides: "preferNativeQuery default true with opt-out"
  key_links:
    - from: "agents/"
      to: "sdk/src/query/"
      via: "Agent markdown uses gsd-sdk query instead of cat/grep/ls for planning reads"
      pattern: "gsd-sdk query"
---

<objective>
Close umbrella issue #2302 by amending four open upstream PRs (#2340 docs/call-sites, #2341 parity
harness + handlers, #2342 runners + fallback, #2343 deprecation header) so full parity, universal
CJS fallback, real tests, documentation architecture, session-ID correlation, and agent determinism
ship inside those PRs — no follow-up Phase 3.5. This plan is the imported master strategy: per-PR
gap analysis, merge order, scope guardrails, and verification checklists. Execution follows
recommended merge order: #2343 (as-is), then #2341, #2342, #2340 — or a documented restack — while
preserving one-concern-per-PR framing.
</objective>

<execution_context>
@D:/Repos/get-shit-done/.cursor/get-shit-done/workflows/execute-plan.md
@D:/Repos/get-shit-done/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Source import:** External plan `comprehensively-review-the-four-iridescent-reef.md` (Claude plans dir).

**Upstream refs:** `gsd-build/get-shit-done` PRs #2340-#2343; issues #2302, #2007.

**User pivots (locked):** (1) Full parity in existing PRs only. (2) Universal CJS fallback for missing
handlers and opt-in execution-fallback. (3) Real tests, not stubs, before merge.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify PR #2343 scope and land as-is</name>
  <files>get-shit-done/bin/gsd-tools.cjs</files>
  <behavior>
    - Confirm four-line @deprecated JSDoc header matches #2302 Track D; no additional edits
  </behavior>
  <action>
    Review diff; merge first as trivial deprecation anchor per strategy doc.
  </action>
  <verify>
    gh pr diff 2343 --repo gsd-build/get-shit-done | head -n 40
  </verify>
  <done>
    Deprecation header-only scope confirmed; PR ready to merge first.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PR #2341 — handlers, golden rows, real tests, matrix fix</name>
  <files>sdk/src/query/**/*.ts, sdk/src/golden/**, sdk/test/**, sdk/vitest.config.ts</files>
  <behavior>
    - Add handlers: state.signal-waiting/resume, phase.list-plans, phase.list-artifacts, plan.task-structure,
      requirements.extract-from-plans, learnings.list/prune/delete, config-path; register and extend QUERY_MUTATION_COMMANDS
    - Replace stub tests in profile.test.ts, skills.test.ts, websearch.test.ts; add e2e.integration.test.ts, errors.test.ts,
      state-mutation concurrent lock test; add c8 coverage floors per doc
    - Fix QUERY-HANDLERS.md matrix to enumerate surfaces precisely
  </behavior>
  <action>
    Implement and land on #2341 branch; expand golden parity rows for new read-only handlers.
  </action>
  <verify>
    npm run build --prefix sdk
    npx vitest run --config sdk/vitest.config.ts --project unit
  </verify>
  <done>
    New handlers registered; golden-policy + unit tests green; QUERY-HANDLERS matrix accurate.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PR #2342 — native default, CLI fallback, session IDs, loadConfig TS port</name>
  <files>sdk/src/phase-runner.ts, sdk/src/init-runner.ts, sdk/src/index.ts, sdk/src/query/registry.ts, sdk/src/cli.ts, sdk/src/gsd-tools.ts, sdk/src/config.ts</files>
  <behavior>
    - Default preferNativeQuery: true on PhaseRunner, InitRunner, GSD; env opt-out
    - CLI: on unregistered handler, shell out to gsd-tools.cjs with documented stderr warning; GSD_QUERY_FALLBACK policy
    - Thread sessionId through createRegistry; remove hardcoded empty sessionId in mutation events
    - Port loadConfig off createRequire path where applicable; tests for fallback and session correlation
  </behavior>
  <action>
    Implement per Universal CJS Fallback Design section in source plan; add registry.test.ts, cli.test.ts, gsd-tools.test.ts coverage.
  </action>
  <verify>
    npm run build --prefix sdk
    npx vitest run --config sdk/vitest.config.ts sdk/src/query/registry.test.ts sdk/src/cli.test.ts sdk/src/gsd-tools.test.ts
  </verify>
  <done>
    Native path default on; CLI fallback tested; sessionId on mutation events; loadConfig path without fragile createRequire where ported.
  </done>
</task>

<task type="auto">
  <name>Task 4: PR #2340 — agents, workflows, SDK doc tree, HANDOVER retirement</name>
  <files>agents/**/*.md, get-shit-done/workflows/plan-phase.md, sdk/README.md, sdk/docs/**, docs/**, CHANGELOG.md</files>
  <behavior>
    - Replace shell/cat/grep patterns in agent markdown with gsd-sdk query per line-item mapping in source plan
    - Create sdk/README.md, sdk/docs/ARCHITECTURE.md, PARITY.md, GOLDEN-TESTS.md; merge or delete HANDOVER-* per rename map
    - Fix misleading matrix language, Windows-only examples, gsd-intel-updater contradictions
  </behavior>
  <action>
    Land after #2341 handlers exist so agent call sites resolve; align CLI-TOOLS and cross-links.
  </action>
  <verify>
    rg "cat .planning/STATE.md" agents/ -n || true
    test -f sdk/README.md &amp;&amp; test -f sdk/docs/PARITY.md
  </verify>
  <done>
    Agent grep checks pass; doc tree files exist; links resolve from sdk/README.md.
  </done>
</task>

<task type="checkpoint">
  <name>Task 5: Verification and dogfood gates</name>
  <files>.planning/ROADMAP.md, CHANGELOG.md</files>
  <behavior>
    - Run Issue #2302 acceptance checklist, full-parity/fallback checklist, agent determinism greps, test quality metrics, optional /gsd-execute-phase dogfood
  </behavior>
  <action>
    Maintainer review of four PRs; CI green Ubuntu Node 22+24 and macOS; confirm no HANDOVER-*.md under sdk/ after publish paths settled.
  </action>
  <verify>
    gh issue view 2302 --repo gsd-build/get-shit-done
  </verify>
  <done>
    All #2302 AC boxes checked; optional dogfood /gsd-execute-phase on scratch clone recorded.
  </done>
</task>

</tasks>
