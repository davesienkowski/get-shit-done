---
phase: quick-260417-k6h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/get-shit-done/**
  - .claude/agents/gsd-*.md
autonomous: false
requirements:
  - QUICK-K6H-01
must_haves:
  truths:
    - "Installed .claude/get-shit-done/workflows/ matches source get-shit-done/workflows/"
    - "Installed .claude/agents/gsd-*.md matches source agents/gsd-*.md"
    - "No installed workflow or agent still calls gsd-tools.cjs directly (all use gsd-sdk query)"
    - "Human has verified a smoke-test workflow reads the refreshed files"
  artifacts:
    - path: ".claude/get-shit-done/workflows/plan-phase.md"
      provides: "Refreshed workflow file (should match source)"
    - path: ".claude/agents/gsd-planner.md"
      provides: "Refreshed planner agent (should match source)"
  key_links:
    - from: ".claude/get-shit-done/workflows/"
      to: "get-shit-done/workflows/"
      via: "bin/install.js copy step"
      pattern: "identical content post-install"
    - from: ".claude/agents/gsd-*.md"
      to: "agents/gsd-*.md"
      via: "bin/install.js copy step"
      pattern: "identical content post-install"
---

<objective>
Re-run the GSD installer against the local project so the stale files under `.claude/get-shit-done/` and `.claude/agents/` are overwritten with the fully-migrated source versions from `get-shit-done/` and `agents/` (which now use `gsd-sdk query` instead of calling `gsd-tools.cjs` directly).

Purpose: The installed harness files are out of sync with the migrated source. Any agent/workflow invocation through Claude Code is still loading the old CJS-calling versions, undoing the Phase 14 migration work from the user's perspective.

Output: Refreshed `.claude/get-shit-done/` and `.claude/agents/` matching current source; a pre/post diff showing stale CJS references removed.
</objective>

<execution_context>
@D:/Repos/get-shit-done/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@D:/Repos/get-shit-done/bin/install.js
@D:/Repos/get-shit-done/.planning/STATE.md

<interfaces>
<!-- bin/install.js entry-point behavior (inferred from help output) -->
Invocation for local Claude Code install:
  `node bin/install.js --local --claude`

Expected behavior:
  - Copies `get-shit-done/**` -> `.claude/get-shit-done/**`
  - Copies `agents/gsd-*.md` -> `.claude/agents/gsd-*.md`
  - Copies `commands/gsd/*.md` -> `.claude/commands/gsd/*.md`
  - May prompt for confirmation; pass `--yes`/`-y` style flag if prompt blocks (check `node bin/install.js --help` tail for the flag).

Current staleness signal:
  - Source files reference `gsd-sdk query` (migrated)
  - Installed files still reference `gsd-tools.cjs` directly (stale)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-install staleness baseline</name>
  <files>
    .planning/quick/260417-k6h-re-run-gsd-installer-to-sync-stale-claud/pre-install-baseline.txt
  </files>
  <action>
    Record evidence that installed files are stale before running the installer, so we can prove the sync worked.

    Run these commands and capture their output into `pre-install-baseline.txt`:

    1. Count stale references in installed workflows:
       `grep -rn "gsd-tools\.cjs" .claude/get-shit-done/workflows/ | wc -l`
    2. Count stale references in installed agents:
       `grep -rn "gsd-tools\.cjs" .claude/agents/ | wc -l`
    3. Count migrated references in source workflows (expected to be higher):
       `grep -rn "gsd-sdk query" get-shit-done/workflows/ | wc -l`
    4. Count migrated references in source agents:
       `grep -rn "gsd-sdk query" agents/ | wc -l`
    5. Diff one representative file to show drift:
       `diff get-shit-done/workflows/plan-phase.md .claude/get-shit-done/workflows/plan-phase.md | head -40`

    Write results into `pre-install-baseline.txt` with clear section headers. Do NOT modify any source or `.claude/` files in this task.

    Why: Provides proof the installer actually changed things (Task 3 diffs against this).
  </action>
  <verify>
    <automated>test -s .planning/quick/260417-k6h-re-run-gsd-installer-to-sync-stale-claud/pre-install-baseline.txt</automated>
  </verify>
  <done>`pre-install-baseline.txt` exists and contains at least the five captured outputs; at least one stale-reference count is greater than zero (if zero everywhere, surface this to user — installer may already be in sync).</done>
</task>

<task type="auto">
  <name>Task 2: Re-run installer for local Claude Code target</name>
  <files>
    .claude/get-shit-done/**
    .claude/agents/gsd-*.md
    .claude/commands/gsd/*.md
  </files>
  <action>
    Execute the local Claude Code install so the migrated source overwrites the stale `.claude/` tree.

    Command (run from repo root `D:/Repos/get-shit-done`):

    ```
    node bin/install.js --local --claude
    ```

    Behavior expectations and handling:

    - If the installer prompts interactively and Bash cannot respond, re-run piping `y` answers:
      `yes y | node bin/install.js --local --claude`
      If that still hangs, consult `node bin/install.js --help` for a non-interactive flag (e.g. `--yes`, `--force`, `--skip-prompts`) and use it. Do NOT fabricate a flag — verify it exists in help output first.
    - Capture the full stdout+stderr to `install-run.log` in the quick-task directory:
      `node bin/install.js --local --claude 2>&1 | tee .planning/quick/260417-k6h-re-run-gsd-installer-to-sync-stale-claud/install-run.log`
    - If installer errors out, surface the error and STOP — do not attempt manual file copies as a workaround (that's the exact drift pattern this task is fixing).

    After the installer completes, immediately run the same grep commands from Task 1 against `.claude/` and append results to `install-run.log` under a `=== POST-INSTALL VERIFICATION ===` heading:
    - `grep -rn "gsd-tools\.cjs" .claude/get-shit-done/workflows/ | wc -l` (expect 0 or drastically lower)
    - `grep -rn "gsd-tools\.cjs" .claude/agents/ | wc -l` (expect 0 or drastically lower)
    - `grep -rn "gsd-sdk query" .claude/get-shit-done/workflows/ | wc -l` (expect non-zero, matching source)
    - `grep -rn "gsd-sdk query" .claude/agents/ | wc -l` (expect non-zero, matching source)
    - `diff get-shit-done/workflows/plan-phase.md .claude/get-shit-done/workflows/plan-phase.md` (expect empty)

    Do NOT commit anything yet — verification happens in Task 3.
  </action>
  <verify>
    <automated>test -s .planning/quick/260417-k6h-re-run-gsd-installer-to-sync-stale-claud/install-run.log && diff -q get-shit-done/workflows/plan-phase.md .claude/get-shit-done/workflows/plan-phase.md</automated>
  </verify>
  <done>Installer ran to completion (log captured); `.claude/get-shit-done/workflows/plan-phase.md` is byte-identical to source; post-install `gsd-tools.cjs` counts under `.claude/` dropped to 0 (or match source counts if source still has any legitimate references).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies sync and approves commit</name>
  <what-built>
    Refreshed `.claude/get-shit-done/` and `.claude/agents/` trees matching migrated source. Pre- and post-install baselines captured in the quick-task directory.
  </what-built>
  <how-to-verify>
    1. Open `.planning/quick/260417-k6h-re-run-gsd-installer-to-sync-stale-claud/pre-install-baseline.txt` and `install-run.log` side by side.
       - Confirm `gsd-tools.cjs` counts under `.claude/` dropped (ideally to 0).
       - Confirm `gsd-sdk query` counts under `.claude/` now match source counts.
       - Confirm the `plan-phase.md` diff is empty post-install.
    2. Spot-check one installed agent file matches its source:
       `diff agents/gsd-planner.md .claude/agents/gsd-planner.md` → expect empty.
    3. Sanity-run `git status` — you should see modifications under `.claude/get-shit-done/` and `.claude/agents/` (the re-sync). Commands under `.claude/commands/gsd/` may also update.
    4. If satisfied, reply `approved` and Claude will commit with message (no co-author tag per CLAUDE.md):
       `chore(installer): re-sync .claude/ harness with migrated gsd-sdk query source`
       Files staged: `.claude/` changes only. The `.planning/quick/260417-k6h-*` artifacts are optional to commit — ask if unsure.
    5. If something looks wrong (e.g., files unchanged, installer skipped a directory), reply with the specific issue and Claude will investigate (likely re-inspect `bin/install.js` copy logic or missing flags).
  </how-to-verify>
  <resume-signal>Type `approved` to commit, or describe the issue to investigate.</resume-signal>
</task>

</tasks>

<verification>
- `diff -rq agents/ .claude/agents/ | grep -v "^Only in agents"` returns no lines mentioning `gsd-` agents (any divergence is a bug).
- `diff -rq get-shit-done/workflows/ .claude/get-shit-done/workflows/` returns no differing lines.
- Under `.claude/`, zero workflows/agents invoke `gsd-tools.cjs` directly unless the source also does (source is the ground truth).
</verification>

<success_criteria>
- Installer executed without error and log captured.
- Installed `.claude/` tree is byte-identical to source for `agents/gsd-*.md` and `get-shit-done/workflows/*.md`.
- Stale `gsd-tools.cjs` references in `.claude/` resolved (match source counts, expected 0 post-migration).
- Human approved the sync.
- Commit landed with no co-author tag.
</success_criteria>

<output>
After completion, create `.planning/quick/260417-k6h-re-run-gsd-installer-to-sync-stale-claud/260417-k6h-SUMMARY.md` recording:
- Installer command actually used (and any non-interactive flag)
- Pre/post grep counts for `gsd-tools.cjs` and `gsd-sdk query`
- Commit SHA
- Any surprises (e.g., files outside `.claude/` that changed, install flags that did not exist)
</output>
