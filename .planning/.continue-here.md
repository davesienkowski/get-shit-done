---
context: research
phase: N/A
task: 2
total_tasks: 4
status: in_progress
last_updated: 2026-04-17T03:02:40.210Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] **CONSTRAINT: stale-hash-scripts-deleted** — The old `scripts/build-2302-track-b-list.mjs`
  and `scripts/rebase-track-b-editor.mjs` have been deleted. They referenced pre-rebase commit
  hashes (0/69 matched current branch). Do NOT attempt to recreate them. Use
  `scripts/split-tracks.mjs` (pattern-based, hash-independent) instead.

- [ ] **CONSTRAINT: track-b-is-unfiltered** — `feat/2302-track-b-registry-docs` currently has
  69 commits vs upstream/main — identical to track-a. It was NOT successfully filtered by the
  previous session's rebase. Do NOT submit this branch as a PR. Build a clean replacement via
  cherry-pick (see remaining_work Task 2).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| stale-hash-split | Splitting branches by commit hash breaks silently after any rebase | advisory | Use `scripts/split-tracks.mjs` (subject-pattern classification) |
| submitting unfiltered track-b | `feat/2302-track-b-registry-docs` looks like a PR branch but has all 69 commits (should be 22) | blocking | Run `git log --oneline upstream/main..feat/2302-track-b-registry-docs \| wc -l` before PR — must be 22 |

<current_state>
**Branch:** `feat/2302-track-a-golden` — working tree clean (71 commits vs upstream/main).

PR prep work for issue #2302 (Phase 3 SDK query contract, golden parity, runners, CJS deprecation).
The maintainer (trek-e) approved all 4 tracks as separate PRs under umbrella #2302.

**Track classification (from `scripts/split-tracks.mjs` run this session):**
- Track A: 47 commits (sdk-scoped — handler implementations, golden tests, HANDOVER docs)
- Track B: 22 commits (docs migration — zh-CN, workflows, references, config, CHANGELOG)
- Track C: 3 commits on `feat/2302-track-c-runners` — ready to PR
- Track D: 1 commit on `feat/2302-track-d-cjs-deprecation` — ready to PR

**Two commits added this session:**
- `24e8b71 feat(sdk): add golden parity harness vs gsd-tools.cjs` (cherry-pick resolved — HEAD had more tests than incoming ceede78; kept HEAD in both conflict hunks)
- `6dfefbd chore(scripts): replace stale hash-based track splitter with subject-pattern classifier`

**Problem with current track branches:**
Both `feat/2302-track-a-golden` and `feat/2302-track-b-registry-docs` contain all 69 A+B commits
mixed. Neither is suitable for PR as-is. Clean branches must be built from upstream/main via
cherry-pick.
</current_state>

<completed_work>

- Task 1 DONE: Identified stale hash problem, deleted old scripts, wrote `scripts/split-tracks.mjs`
- Task 1 DONE: Resolved cherry-pick conflict in `golden.integration.test.ts` (kept HEAD — more tests)
- Task 1 DONE: Committed golden parity harness (24e8b71) and split-tracks tooling (6dfefbd)
- Classification verified: 47 Track A + 22 Track B = 69 ✓

</completed_work>

<remaining_work>

## Task 2 — Build clean Track B branch (22 docs-migration commits)

```bash
# Confirm hashes still correct (run from feat/2302-track-a-golden)
node scripts/split-tracks.mjs

# Build clean branch
git checkout -b feat/2302-track-b-registry-docs-v2 upstream/main
git cherry-pick \
  cf4650edd044792fa80b8da40b0136a250f5e8ae \
  6a6acce5b29b680c464eb6c46abf90feb75aeb58 \
  20cf24b60c98b06aecf1ea3451142a77a02d9677 \
  43254db2b8ccb8b15bf3c2db06b7e2d4e5ffdb4f \
  f424689e99ca9965dd322248b89bf7bb33c806ae \
  b13fc4760a98ca47954621fdb8a9fb7fee7840c3 \
  4f01a3434ba5e9a6b2de297a3c3606e3effd267d \
  09d69f51db5c492660aa320455e59eda6d930446 \
  731effda9fd33246335434089fd24d147051f971 \
  7065d9c8a39a5c64127d8ee731664d75e9d9e457 \
  4df0b10edc3d7b044f62ada4191fc3147aa6f720 \
  95d6143d7ecb3aeaa911fd72d9e2e9e5191a2f87 \
  cfd43e818cb6f213860a061190c5ae0c70502873 \
  39bc4b85359823104e5e4b31785f971a0f1d8aa2 \
  a69a6ed53df59da814b81c50a384522eb8d12b50 \
  8e98c275bd37456bea5d29c14cb12872b5a8ba00 \
  8ba4e15523a5f231f1dec0c57c3c16f738e180a9 \
  2d61d00ba1ae4aa7892ffb11e3d11b8dcb4459df \
  6bf3f9015b904b107b39f95518bb8e41cd720c0c \
  b1bbf9dff34e0b6f6ff6ae89c185f80e70d05d62 \
  23e449806854fa11260857e8d39eba0c6a9f220a \
  ed11cddf23960f6e2df932fd950c345e7a6adcbe

# Verify count
git log --oneline upstream/main..feat/2302-track-b-registry-docs-v2 | wc -l  # → 22
```

## Task 3 — Build clean Track A branch (47 sdk-scoped commits)

```bash
# Get commands from script (preferred — in case hashes changed)
node scripts/split-tracks.mjs

git checkout -b feat/2302-track-a-golden-v2 upstream/main
git cherry-pick <47 A hashes from script output>

# Verify count + run tests
git log --oneline upstream/main..feat/2302-track-a-golden-v2 | wc -l  # → 47
cd sdk && npm test
```

## Task 4 — Submit 4 PRs in order

1. Track B (`feat/2302-track-b-registry-docs-v2`) — docs migration, no implementation deps
2. Track A (`feat/2302-track-a-golden-v2`) — golden tests + handler implementations
3. Track C (`feat/2302-track-c-runners`) — runner alignment (3 commits, already clean)
4. Track D (`feat/2302-track-d-cjs-deprecation`) — CJS header deprecation (1 commit, already clean)

All PRs reference umbrella issue #2302.

</remaining_work>

<decisions_made>

- **Pattern-based classification over hash-based**: `scripts/split-tracks.mjs` uses subject
  patterns (type=sdk OR scope contains "sdk" → Track A; QUERY-HANDLERS reference → Track A;
  else → Track B). Hash-independent and survives future rebases.

- **Cherry-pick onto upstream/main** supersedes previous rebase-drop approach: explicit,
  auditable, no risk of carrying wrong commits.

- **PR ordering B → A → C → D**: B (docs) has no deps. A (golden tests) references handlers
  B documents. C and D are standalone.

- **`feat/2302-track-a-golden` is source-of-truth** for classification. Do not submit it directly.

</decisions_made>

<blockers>
None. All tooling committed. Proceed with cherry-pick tasks.
</blockers>

## Required Reading (in order)
1. `scripts/split-tracks.mjs` — run first to confirm classification is still 47/22 with current hashes
2. Check upstream: `git log upstream/feat/2302-feat-sdk-phase-3-sdk-query-contract-gold --oneline | head -5` — the maintainer's own track-a branch; verify no divergence before PR

## Infrastructure State
- Branch: `feat/2302-track-a-golden` — clean, 71 commits vs upstream/main
- `feat/2302-track-b-registry-docs` — unfiltered (69 commits, do NOT use for PR)
- `feat/2302-track-c-runners` — 3 commits vs upstream, clean and ready
- `feat/2302-track-d-cjs-deprecation` — 1 commit vs upstream, clean and ready
- upstream remote: `gsd-build/get-shit-done`
- origin remote: `davesienkowski/get-shit-done`

<context>
The session fixed a broken split strategy (stale hashes) and built replacement tooling.
The cherry-pick in progress from the previous session (golden.integration.test.ts conflict)
was resolved — HEAD had more tests than the incoming commit, so kept HEAD in both hunks.
Two commits were made. The source-of-truth branch is now clean and correct.
The remaining work is mechanical: cherry-pick two filtered sets onto fresh branches,
run tests on Track A, then submit 4 PRs.
</context>

<next_action>
Start with: `node scripts/split-tracks.mjs` on branch `feat/2302-track-a-golden` to confirm
classification is still 47/22, then build the clean Track B branch
(`feat/2302-track-b-registry-docs-v2`) using the cherry-pick command in Task 2.
</next_action>
