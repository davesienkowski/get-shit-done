---
context: default
phase: 14-composition-and-retirement
task: 4
total_tasks: 4
status: paused
last_updated: 2026-04-17T18:17:14.495Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_If no constraints have been identified yet, remove this section._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| _None logged this session_ | — | — | — |

_Remove rows that do not apply._

<current_state>

**Milestone v3.0** is marked **complete** in `.planning/STATE.md` (Phase 14 — composition-and-retirement all plans done). There is **no active** `phases/*/PLAN.md` detected in this workspace snapshot, so this handoff lives at **`.planning/.continue-here.md`** (default).

**Git:** `main` includes merged **#2302** tracks A–D and commit **`f103cf4`** (CodeRabbit follow-ups). Local branch may be **ahead/behind** `upstream/main` — reconcile before pushing to upstream.

**Upstream PRs (gsd-build/get-shit-done):** **#2340** (#2302 Track B), **#2341** (Track A), **#2342** (Track C), **#2343** (Track D) were **open** with CI green; refresh branch tips with latest `main` if you want new commits reviewed.

</current_state>

<completed_work>

Completed Tasks:

- Merged **feat/2302-track-b-registry-docs-v2**, **feat/2302-track-c-runners**, **feat/2302-track-d-cjs-deprecation** into `main` after prior Track A merge; pushed `main` to **origin**.
- Implemented **CodeRabbit** follow-ups: deterministic VERIFICATION/UAT file pick in `check-completion.ts`, `statePlannedPhase` validation, `statePrune` `keep-recent` validation, `verify.commits` golden test using `git rev-list` (commit **f103cf4**).
- Confirmed **`gsd-sdk query state load`** JSON matches **`gsd-tools.cjs state load`** (non-`--raw`); documented `--raw` vs JSON difference.

</completed_work>

<remaining_work>

- Merge or cherry-pick **`f103cf4`** (and any newer `main`) into each **PR branch** on the fork, then push, so GitHub/CodeRabbit see the latest fixes.
- Restore **`sdk/src/golden/capture.ts`** in the repo if missing locally — integration test **`read-only-parity.integration.test.ts`** imports it.
- **Sync** with **upstream** (`git fetch upstream` / merge or rebase) to clear ahead/behind drift before upstream merge.

</remaining_work>

<decisions_made>

- Default handoff path **`.planning/.continue-here.md`** because milestone is complete and no active phase plan directory was selected for this pause.

</decisions_made>

<blockers>

- None technical for resume; upstream merge order and review remain **process** items.

</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` — current milestone and last activity
2. `.planning/PROJECT.md` — initiative scope
3. `sdk/src/query/QUERY-HANDLERS.md` — SDK query contracts (if touching query layer)

## Critical Anti-Patterns (do NOT repeat these)

- Relying on **identical SHAs** in `verify.commits` golden test — use two distinct commits (`rev-list`).

## Infrastructure State

- No dev servers or watchers were left running for this workflow.

## Pre-Execution Critique Required

_Not applicable (not pausing between spike design and execution)._

<context>

Pause captures **post–Phase 3** integration work: **four-track merge**, **CodeRabbit** remediation, **parity** verification.

</context>

<next_action>

Start with: **`/gsd-resume-work`** or **`git fetch upstream && git merge upstream/main`** (or rebase), then update PR branches **#2340–#2343** with latest `main` and push.

</next_action>
